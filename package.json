{
  "private": true,
  "scripts": {
    "start": "npm run server && npm run client",
    "client": "cd client && npm run start",
    "server": "cd server && npx ts-node -r tsconfig-paths/register src/App.ts --files",
    "deploy": "npm install && cd client && npm install && cd ../server && npm install",
    "cbuild": "cd client && npm run build"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "name": "gobang",
  "version": "1.0.0",
  "description": "gobang demo",
  "main": "None",
  "author": "fls",
  "license": "ISC",
  "dependencies": {
    "winston": "^3.8.2"
  }
}
