{
  "private": true,
  "scripts": {
    "start": "npm run server && npm run client",
    "client": "cd client && npm run start",
    "server": "cd server && ts-node App.ts",
    "deploy": "cd client && npm install && cd ./server && npm install",
    "cbuild": "cd client && npm run build"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "name": "gobang",
  "version": "1.0.0",
  "description": "gobang demo",
  "main": "None",
  "author": "fls",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.5.4",
    "winston": "^3.8.2"
  }
}
